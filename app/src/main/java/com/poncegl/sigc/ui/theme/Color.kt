package com.poncegl.sigc.ui.theme

import androidx.compose.ui.graphics.Color

// Light Mode Colors
val PrimaryLight = Color(0xFF1daf8d)           // Teal accent
val PrimaryVariantLight = Color(0xFF5FCBC0)    // Lighter teal
val SecondaryLight = Color(0xFFf9fafb)         // Blue accent
val BackgroundLight = Color(0xFFF5F8F7)        // Very light gray/off-white
val SurfaceLight = Color(0xFFFFFFFF)           // Pure white for cards
val SurfaceVariantLight = Color(0xFFF0F4F3)    // Slightly tinted surface
val OnPrimaryLight = Color(0xFFFFFFFF)         // White text on primary
val OnSecondaryLight = Color(0xFFF3F3F3)       // White text on secondary
val OnBackgroundLight = Color(0xFF1A2E2E)      // Dark text on background
val OnSurfaceLight = Color(0xFF14181f)         // Dark text on surface
val OnSurfaceVariantLight = Color(0xFF676f7e) // Medium gray text

// Dark Mode Colors
val PrimaryDark = Color(0xFF1daf8d)            // Teal accent (same as light)
val PrimaryVariantDark = Color(0xFF5FCBC0)     // Lighter teal (same as light)
val SecondaryDark = Color(0xFF5BA3F5)          // Lighter blue for dark mode
val BackgroundDark = Color(0xFF101318)         // Dark teal/green
val SurfaceDark = Color(0xFF1F5555)            // Slightly lighter teal for cards
val SurfaceVariantDark = Color(0xFF26605E)     // Card variant
val OnPrimaryDark = Color(0xFFFFFFFF)          // White text on primary
val OnSecondaryDark = Color(0xFFFFFFFF)        // White text on secondary
val OnBackgroundDark = Color(0xFFE8F5F4)       // Light text on background
val OnSurfaceDark = Color(0xFFFFFFFF)          // White text on surface
val OnSurfaceVariantDark = Color(0xFF8FABA8)   // Medium gray/teal tinted text

// Status Colors (same for both themes, used with proper backgrounds)
val ErrorLight = Color(0xFFD64550)             // Red for errors/negative
val ErrorDark = Color(0xFFE85865)              // Lighter red for dark mode
val SuccessLight = Color(0xFF4CAF50)           // Green for success/positive
val SuccessDark = Color(0xFF66C96A)            // Lighter green for dark mode
val WarningLight = Color(0xFFF5A623)           // Orange/yellow for warnings
val WarningDark = Color(0xFFFFB84D)            // Lighter orange for dark mode
val InfoLight = Color(0xFF4A90E2)              // Blue for info
val InfoDark = Color(0xFF5BA3F5)               // Lighter blue for dark mode